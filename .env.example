DOMAIN=localhost
PROTOCOL=http

# Public Vite build-time default. Rebuild the app/image after changing it.
# Runtime URL selection can be enabled or disabled in src/lib/backend/config.ts.
PUBLIC_API_URL=https://audiostream.pxr.dpdns.org
