services:
  web:
    image: ghcr.io/pxr05/cadence:latest
    restart: always
    environment:
      BODY_SIZE_LIMIT: Infinity
      PROTOCOL: ${PROTOCOL}
      ORIGIN: ${PROTOCOL}://${DOMAIN}
      VITE_API_URL: ${VITE_API_URL}
    ports:
      - "6128:3000"